#include<stdio.h>

int main()
{
int add, num1, num2;
printf("HEllo Pranav");

printf("\nEnter first number to perform addition:");
scanf("%d", &num1);

printf("\nEnter second number to perform addition:");
scanf("%d", &num2);
add = num1 + num2;

printf("\naddition of two numbers is :%d",add);

return 0;
}